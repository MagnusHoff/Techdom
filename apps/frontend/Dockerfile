# syntax=docker/dockerfile:1
# Placeholder Dockerfile for the future Next.js App Router frontend service.
# The install/build steps will be fleshed out once package manifests land in this directory.
FROM node:20-alpine AS base

WORKDIR /app

COPY . .

CMD ["npm", "run", "dev"]
